apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-pv-config
  namespace: media
  labels:
    app: prowlarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi