apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-pv-config
  namespace: media
  labels:
    app: overseerr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi