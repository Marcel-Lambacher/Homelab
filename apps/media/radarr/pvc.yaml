apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-pv-config
  namespace: media
  labels:
    app: radarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi